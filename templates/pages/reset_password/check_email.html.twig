{% extends 'base.html.twig' %}

{% block title %}Password Reset{% endblock %}

{% block body %}

    <article>
        <p>If an account matching your email exists, then an email was just sent that contains a link that you can use to reset your password.</p>
        <p>This link will expire in {{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}.</p>
        <p>If you don't receive an email please check your spam folder or</p>
        <a class="a-button" href="{{ path('app_forgot_password_request') }}">Try again</a>
    </article>

{% endblock %}