<header>
    <nav>
        <ul>
            <h1><a href="{{ path('app_main_index') }}">LoliSSR</a></h1>
            <li><a href="{{ path('app_contact_create') }}">Contact</a></li>

            {% if app.user %}
                {% if is_granted('ROLE_USER') %}
                    <li><a href="{{ path('app_main_index') }}">User</a></li>
                {% endif %}
                {% if is_granted('ROLE_ADMIN') %}
                    <li><a href="{{ path('app_admin') }}">Admin</a></li>
                {% endif %}
            {% endif %}
        </ul>
        <ul>
            {% if app.user %}
                {% if is_granted('ROLE_USER') %}
                    <li><a href="{{ path('app_profil') }}">{{ app.user.name }}</a></li>
                    <li><a href="{{ path('app_logout') }}">Logout</a></li>
                {% endif %}
            {% else %}
                <li><a href="{{ path('app_register') }}">Register</a></li>
                <li><a href="{{ path('app_login') }}">Login</a></li>
            {% endif %}
        </ul>
    </nav>
</header>